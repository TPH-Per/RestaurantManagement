CREATE TABLE Staff ( staff_id BIGINT IDENTITY(1,1) PRIMARY KEY, full_name NVARCHAR(100) NOT NULL, email NVARCHAR(100) NOT NULL, phone NVARCHAR(20), password NVARCHAR(100) NOT NULL, role NVARCHAR(20) NOT NULL, is_active BIT NOT NULL, created_at DATETIME2 NOT NULL );