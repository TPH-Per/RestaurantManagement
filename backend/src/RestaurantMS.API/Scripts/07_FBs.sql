CREATE TABLE FBs ( fb_id BIGINT IDENTITY(1,1) PRIMARY KEY, name NVARCHAR(100) NOT NULL, price DECIMAL(18,2) NOT NULL, type NVARCHAR(20) NOT NULL, category_id BIGINT NOT NULL, manufacturer_id BIGINT, is_visible BIT NOT NULL, unit NVARCHAR(50), description NVARCHAR(500) );